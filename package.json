{
  "devDependencies": {
    "@rollup/plugin-typescript": "^11.1.5",
    "rollup": "^4.9.2",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "build": "rollup -c rollup.config.mjs && cp src/manifest.json build/manifest.json && cp -r assets build/icons"
  }
}
